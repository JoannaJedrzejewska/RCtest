# tests/testthat.R
#
# Test runner discovered by R CMD check and devtools::test().
# Keep this file directly inside tests/, not inside tests/testthat/.

library(testthat)
library(RCtest)

test_check("RCtest")
